using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TroopAnimatorHandler : AnimatorHandler
{
   /* Animator anim;s
    //
    private void Awake()
    {
        anim = GetComponentInChildren<Animator>();
    }

    public new void PlayTargetAnimation(string animation, bool interacting)
    {
      //  anim.SetBool("isInteracting", interacting);
      //  anim.Play(animation);
    }

    public new bool GetIsInteracting() => anim.GetBool("isInteracting");*/
}
